# 2026-08-27 工作记录

## 短剧网站（duanju.hhtc.top）迁移合并到本目录

- **迁移源**：`/Volumes/brainos/website/xiaoshuo/workbuddy/2026-08-25-08-13-09/`（原"短剧网站"项目，本目录原为空的"短剧空间"工作目录）
- **方式**：rsync 复制，排除 node_modules / .workbuddy / index.html.bak / .DS_Store / *.log / data/posts.json
- **结果**：
  - 代码齐全：index.html (142KB)、admin.html、ai-creation-studio.html、server.js、seed.js、deploy.sh、data/（channels/config/usage.json）、package.json、overview.md、CHANGELOG.md
  - .git 历史完整保留（最新 v1.0.41 批量图片生成）
  - npm install 成功（唯一依赖 sharp ^0.35.3）
- **本地验证**：`env -u NODE_OPTIONS node server.js`（PORT 3000）
  - index/admin/studio 全部 200，/api/version 返回 v1.0.40 JSON 正常
  - 注意：本机 NODE_OPTIONS 带 `--use-system-ca`，node 22.12 不认，启动必须 `env -u NODE_OPTIONS`（与 deploy.sh 同款处理）
- **部署链路**：deploy.sh → scp 到 root@lostmusic.top:/root/duanju-app → pm2 restart duanju-app → 线上 https://duanju.hhtc.top
- **旧目录处置**：2026-08-25-08-13-09 暂保留未删，待钱总监确认后处理
- **待办**：Agnes AI API Key 已失效（见旧项目 memory），需要更新 /root/duanju-app/tiangong_gen.js 和 daily_gen.js 第 6 行

## 网站优化（v1.0.42）
### 服务端
- **静态文件 ETag 缓存**：serveStatic 基于 mtime+size 生成 ETag，支持 If-None-Match 304 响应；浏览器刷新不再重复下载 ~142KB HTML
- **HEAD 请求支持**：handleAPI 和 serveStatic 均接受 HEAD，解决此前 HEAD 返回 404 的问题

### 前端
- **Google Fonts 加载优化**：从 CSS `@import`（串行阻塞）改为 `<link rel="preconnect">` + `<link>`，首屏渲染不再等字体文件解析完
- **视频轮询动态退避**：从固定 8s setInterval 改为动态 setTimeout 退避（4s→8s→12s→上限 16s），生成完成更快感知，同时降低服务端压力

### 验证
- 本地 3000 端口：首次 GET 200 + ETag，二次带 ETag 请求 304；HEAD 全部 200；index.html 141956B
- git commit 已 stage，等待确认版本号和说明后 commit + deploy

### 改动文件
- server.js（serveStatic 重构，支持 ETag + HEAD）
- index.html（字体加载优化 + 轮询动态退避）

## 图片生成任务修复与执行（2026-08-27）

### 问题诊断
- **现象**：daily_gen.js 和 tiangong_gen.js 执行失败，返回"无效的令牌"
- **根本原因**：channels.json 文件被清空（0字节），导致批量生成接口无法读取 API Key
- **修复**：从 channels.json.bak 恢复并写入 API Key `cpk-jE3Qzzq...`

### 执行结果
- **日常美女图**：34张成功 / 28次失败（速率限制）
- **天宫神话图**：47张成功 / 131次失败（速率限制）
- **失败原因**：Agnes AI 免费额度限制，"4K tier allows 1 request per 1 minute"
- **缓存图片**：69张 → 82张（+13张）
- **总帖子数**：344张 → 416张（+72张）
- **今日生成**：25张 → 97张（+72张）

### 发现的问题
- PM2 日志有 `ERR_HTTP_HEADERS_SENT` 错误（pre-existing bug，不影响功能）
- config.json 中 agnesApiKey 为空，但 channels.json 中的 channel apiKey 已配置，可正常运行

