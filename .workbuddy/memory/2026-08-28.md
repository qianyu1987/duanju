# 2026-08-28 工作记录

## v1.2 角色一致性系统部署验证

### 功能实现状态
1. **角色库管理** (`role-lib`)
   - ✓ 角色库面板，位于短剧工坊下方
   - ✓ 支持创建/编辑/删除角色卡片
   - ✓ 每个角色可上传参考图（base64 存储到 localStorage）
   - ✓ 角色名称 + 外貌特征描述

2. **角色选择与预览**
   - ✓ 点击角色卡片可选中/取消选中
   - ✓ 已选角色在剧本生成区上方显示预览卡片
   - ✓ 预览显示角色头像、名称、描述摘要

3. **剧本生成时自动关联角色**
   - ✓ `genDrama()` 函数增加角色上下文注入
   - ✓ 已选角色的名称和描述自动添加到 system prompt
   - ✓ AI 生成剧本时会参考角色设定保持一致性

4. **角色展示优化**
   - ✓ 剧本生成后显示角色卡片，自动匹配角色库参考图
   - ✓ 有图片的角色显示缩略图，无图片显示默认图标

### 技术细节
- localStorage key: `wb_aistudio_role_lib`
- 角色数据结构: `{ id, name, desc, image, createdAt }`
- 参考图存储: base64 编码

### 部署验证
- 远程部署: https://duanju.hhtc.top/api/version 返回 1.2.0 → 1.3.0
- HTML 字节数: 163,715 bytes (v1.3)
- 角色库相关代码: 6处"角色库"，16处"role-lib"

---

## v1.3 分镜生成与可视化

### 功能实现
1. **自动分镜生成** (`autoGenerateStoryboard`)
   - AI 根据剧本自动生成 JSON 格式分镜脚本
   - 支持解析剧本中的场景标记（【场景X】等）
   - 输出镜头类型、角度、时长等元数据

2. **分镜可视化编辑** (`renderStoryboard`)
   - 卡片式分镜列表，每镜显示：编号、镜头类型、角度、时长、描述
   - 支持单镜图片生成（调用图片 API）
   - 支持编辑、删除分镜

3. **批量图片生成** (`generateStoryboardImage`)
   - 逐镜调用图片 API 生成参考图
   - 使用 `image_prompt` 字段控制生成风格

4. **分镜导出** (`exportStoryboard`)
   - 导出为 JSON 文件，包含完整分镜数据

### 新增代码位置
- CSS 样式: 第 740-788 行（`.storyboard-*` 系列）
- UI 组件: 第 1117-1134 行（分镜编辑器区块）
- 核心函数: 第 2579-2800 行

### 部署状态
- 版本: v1.3.0
- 部署成功: https://duanju.hhtc.top
- PM2 进程: online

---

## v1.4 图片动画化（分镜视频生成）

### 功能实现
1. **单镜视频生成** (`generateStoryboardVideo`)
   - 为单个分镜生成短视频（基于参考图）
   - 自动调用视频 API 提交任务
   - 轮询检查生成状态，最多等待 5 分钟

2. **批量视频生成** (`generateAllVideos`)
   - 一键生成所有分镜的视频
   - 进度条显示实时进度
   - 自动跳过已有视频的分镜
   - 失败单个分镜不影响其他分镜继续生成

3. **视频状态追踪** (`pollStoryboardVideo`)
   - 每 5 秒轮询一次 API
   - 更新 UI 显示进度百分比
   - 完成后自动渲染视频播放器

### 技术细节
- 视频生成参数：ratio="9:16", len=5秒
- 参考图作为首帧输入（第一张为主帧）
- 使用 Agnes AI 视频生成 API
- localStorage 存储视频 URL

### 部署状态
- 版本: v1.4.0
- 部署成功: https://duanju.hhtc.top
- HTML 大小: 169,736 bytes

### 下一步
- v1.5: 配音与字幕 ✅（已部署）
- v1.6: 成片输出与分享

---

## v1.5 配音与字幕

### 功能实现
1. **单镜配音** (`generateStoryboardAudio`)
   - 为单个分镜调用 TTS API 生成音频
   - 自动提取场景描述中的对话内容
   - 音频 URL 存储到分镜数据，支持播放

2. **批量配音** (`generateAllAudio`)
   - 一键为所有分镜生成配音
   - 进度条显示实时进度
   - 自动跳过已有音频的分镜

3. **批量字幕生成** (`generateAllSubtitles`)
   - AI 根据场景描述生成精简字幕
   - 字幕以文字形式显示在分镜卡片底部

4. **TTS API 接口** (`apiTTS`)
   - 占位实现，当前返回 null + 提示信息
   - 后续可接入 Azure TTS / 讯飞 / 阿里等真实服务

### 新增代码位置
- CSS 样式: 第 773-798 行（`.storyboard-dialogue`, `.storyboard-subtitle`, `.storyboard-audio-btn`）
- UI 按钮: 第 1163-1164 行（批量配音、批量字幕按钮）
- 核心函数: 第 3076-3295 行

### 部署状态
- 版本: v1.5.0
- 部署成功: https://duanju.hhtc.top/api/version 返回 1.5.0
- PM2 进程: online
- HTML 大小: 190,422 bytes

---

## v1.6 成片输出与分享

### 功能实现
1. **成片预览** (`previewFinalCut`)
   - 全屏暗色模态框，9:16 比例播放器
   - 按顺序自动播放每个分镜（视频优先，无视频则显示图片）
   - 自动跳过每镜时长，支持手动切镜
   - 字幕叠加显示在画面底部

2. **导出文本剧本** (`exportAsText`)
   - 导出为 UTF-8 纯文本，含标题、元信息、每镜描述/对白/字幕

3. **生成分享链接** (`shareStoryboard`)
   - 将分镜数据存入 localStorage
   - 生成带分享 key 的 URL，通过 clipboard API 复制
   - 接收方打开链接自动导入分镜脚本

4. **导出 HTML 预览页** (`exportAsHtml`)
   - 生成独立 HTML 文件，内嵌 CSS，可在浏览器离线查看
   - 按镜显示描述、元数据、对白、字幕、图片/视频

### 新增代码位置
- UI 按钮: 第 1166-1168 行
- 核心函数: 第 4009-4350 行

### 部署状态
- 版本: v1.6.0
- 部署成功: https://duanju.hhtc.top/api/version 返回 1.6.0
- PM2 进程: online
- HTML 大小: 201,949 bytes

---

## 下周计划（2026-08-29 ~ 2026-09-04）

### P0：TTS 真实接入（完成配音功能闭环）
- 对比接入方案：讯飞 TTS / Azure Neural TTS / 阿里智能语音
- 选定服务商，申请 API Key 写入 data/config.json
- 实现 apiTTS() 真实调用，返回音频 URL
- 测试单镜/批量配音端到端流程
- 更新 HTML 中 TTS 占位提示信息

### P1：成片预览优化
- 添加暂停/继续按钮
- 添加整体进度条（已播镜头数/总镜头数）
- 允许手动拖动跳转到指定镜
- 导出 HTML 预览页图片懒加载优化

### P2：服务端稳定性
- 排查 ERR_HTTP_HEADERS_SENT 偶发错误原因
- 调查 channels.json apiKey 字段被意外清空的问题
- 完善 admin.html 渠道管理界面

### P3：用户体验打磨
- 分享链接导入后增加"一键生成分镜"快捷入口
- 分镜编辑器支持拖拽调整镜头顺序
- 批量视频生成添加"暂停/取消"功能

### 待定：v1.7 成片合并（可选）
- 探索 FFmpeg.wasm 前端拼接或后端 ffmpeg 合成
- 将各镜视频拼接为完整短剧 mp4

---

## 目标版本
| 目标 | 预估 |
|------|------|
| v1.6.1（TTS 接入 + 成片预览优化） | 周三前 |
| v1.7.0（成片合并，可选） | 周五前 |

---

## 备注
- 本项目已实现完整分镜→成片工作流（角色→剧本→分镜→图片→视频→配音→字幕→导出），下一步重点是**串联成端到端可交付的成片**
- 若用户反馈某功能使用率低，可考虑在下周做数据埋点或调整入口位置
